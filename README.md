change the file saving format to numbers
encryt the stored email 
get the date and time to specify the event time
duplicate in sponsors get email id in sponsors and validation
set email id to booth,speaker and all
recheck ticket for errors
add category column to expenses


use handler instead of if else

get data the once and store it all for use

declare all the variable at top


Sriram

use strong encryption algorithm

